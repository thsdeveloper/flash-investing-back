// This route will be created later - for now it's a placeholder
import { FastifyPluginAsync } from 'fastify';

const pluggyRoutes: FastifyPluginAsync = async function (fastify) {
  // TODO: Implement pluggy integration routes
  // This will be migrated from src/routes/pluggy/
};

export default pluggyRoutes;
export const autoPrefix = '/pluggy';