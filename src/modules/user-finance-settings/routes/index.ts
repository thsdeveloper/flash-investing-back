// This route will be created later - for now it's a placeholder
import { FastifyPluginAsync } from 'fastify';

const userFinanceSettingsRoutes: FastifyPluginAsync = async function (fastify) {
  // TODO: Implement user finance settings routes
  // This will be migrated from src/routes/user-finance-settings/
};

export default userFinanceSettingsRoutes;
export const autoPrefix = '/user-finance-settings';